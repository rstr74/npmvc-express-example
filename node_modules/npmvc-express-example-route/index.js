module.exports = function(puremvc, facade, config) {
	// include class
	puremvc.include("RouteMediator",__dirname+"/src/view");

	// init class and register to facade
	var routeMediator = new npmvc.view.mediator.RouteMediator();
	routeMediator.init(config);
	facade.registerMediator(routeMediator);

}