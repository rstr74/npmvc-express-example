module.exports = function(include, puremvc) {

	puremvc.define(
		// CLASS INFO
		{
			name: 'npmvc.AppConstants'
		},
		// INSTANCE MEMBERS
		{},
		// CONSTANTS
		{
			STARTUP: 'startup'
			
		});

}